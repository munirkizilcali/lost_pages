<h1>Lost Pages Users</h1>

<ul>
<% @users.each do |user| %>
	<li> <%= link_to user.name, user_path(user) %> - <%=link_to "Add Friend", friendships_path(:friend_id => user.id, :user_id => current_user.id), method: "POST" %>
	 </li>
<% end %>
</ul>
